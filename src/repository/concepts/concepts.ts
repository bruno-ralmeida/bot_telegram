import { DataFormat } from '../../helpers';

const data: DataFormat[] = [
  { key: 'Array', value: 'Lista' },
  { key: 'Array', value: 'Lista' },
  { key: 'Array', value: 'Lista' },
];

export default data;
